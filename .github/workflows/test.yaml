on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: test variables

jobs:

  test:
    runs-on: ubuntu-latest
    
    env: 
      RMD_PATH: 'hello world'

    steps:
        
      - name: test
        run: echo $RMD_PATH
        
      - name: test2
        run: echo $RMD_PATH
